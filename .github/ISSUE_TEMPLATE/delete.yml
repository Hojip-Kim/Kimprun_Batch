name: "DELETE: 제거"
description: 불필요한 코드나 기능 제거를 위한 이슈입니다.
title: "[DELETE] "
labels: ["delete"]
body:
  - type: input
    id: summary
    attributes:
      label: 🗑️ 요약
      description: 무엇을 제거할 예정인가요?
      placeholder: 예) 사용하지 않는 배치 Job 제거
    validations:
      required: true

  - type: textarea
    id: target
    attributes:
      label: 🎯 제거 대상
      description: 구체적으로 무엇을 제거할 건가요?
      placeholder: |
        - LegacyCoinSyncJob.java
        - OldExchangeReader.java
        - 관련 설정 파일들
    validations:
      required: true

  - type: textarea
    id: reason
    attributes:
      label: 📌 제거 이유
      description: 왜 제거해야 하나요?
      placeholder: |
        - 더 이상 사용하지 않는 배치 Job
        - 새로운 배치로 대체됨
        - 성능 저하 요인
        - 유지보수 비용 증가
    validations:
      required: true

  - type: textarea
    id: impact
    attributes:
      label: ⚠️ 영향 범위
      description: 제거 시 영향받는 부분이 있나요?
      placeholder: |
        - 의존하는 다른 배치 Job이 있는지 확인 필요
        - 스케줄러 설정 제거 필요
        - 데이터베이스 정리 필요 여부
    validations:
      required: true

  - type: checkboxes
    id: checklist
    attributes:
      label: ✅ 체크리스트
      options:
        - label: 의존성 확인 완료
        - label: 영향 범위 파악 완료
        - label: 백업 계획 수립
        - label: 롤백 계획 수립
